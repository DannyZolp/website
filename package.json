{
  "name": "website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "nodemon --exec \"tsc && vite build && wrangler pages dev\" --watch \"./src\"",
    "vite:dev": "vite",
    "vite:build": "tsc && vite build",
    "vite:preview": "vite preview",
    "wrangler:types": "wrangler types ./functions/types.d.ts",
    "wrangler:deploy": "wrangler deploy",
    "wrangler:dev": "wrangler pages dev"
  },
  "devDependencies": {
    "nodemon": "^3.1.14",
    "typescript": "~6.0.2",
    "vite": "^8.1.1",
    "wrangler": "^4.114.0"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/addon-web-links": "^0.12.0",
    "@xterm/xterm": "^6.0.0",
    "date-fns": "^4.4.0"
  }
}
